syntax = "proto3";

import "google/api/annotations.proto";

service Reverse {
	rpc Echo (Message) returns (Message) {
		option (google.api.http) = {
			post: "/echo",
			body: "*"
		};
	}
}

message Message {
	string value = 1;
}

